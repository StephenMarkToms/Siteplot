const state = {
    canClose: true,
    show: false,
}
const getters = {}
const actions = {
    test(context) {
        console.log('test')
    },
}
const mutations = {}

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations,
}
